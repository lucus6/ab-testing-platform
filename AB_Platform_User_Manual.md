# DataFly AB 实验平台 — 功能说明书

> **版本：** v3.0 | **更新日期：** 2026-06-05 | **总代码量：** ~2,100 行 Python

---

## 目录

1. [平台概述](#1-平台概述)
2. [用户系统](#2-用户系统)
3. [环境准备与启动](#3-环境准备与启动)
4. [页面一：实验管理](#4-页面一实验管理)
5. [页面二：实验详情](#5-页面二实验详情)
6. [页面三：结果分析](#6-页面三结果分析)
7. [完整操作流程示例](#7-完整操作流程示例)
8. [AB 实验实践指南](#8-ab-实验实践指南)
9. [技术架构](#9-技术架构)
10. [数据唯一性控制](#10-数据唯一性控制)
11. [数据库表结构](#11-数据库表结构)
12. [常见问题](#12-常见问题)

---

## 1. 平台概述

**DataFly** 是一个模拟互联网大厂 AB 实验系统的练习工具，覆盖实验全生命周期：**创建实验 → 流量分桶 → 数据模拟 → 指标计算 → 统计检验 → 可视化分析**。

### 平台特点

- **多用户隔离**：每个人员注册自己的账号，实验数据完全隔离
- **业务编号**：每个实验自动生成唯一编号 `EXP-{用户}-{日期}-{序号}`
- **零配置数据库**：SQLite 本地存储，启动即可用

### 核心模块（7 个）

| 模块 | 文件 | 功能 |
|------|------|------|
| 用户认证 | `services/auth_service.py` | 注册/登录/密码哈希(PBKDF2-SHA256) |
| 实验管理 | `services/experiment_service.py` | 实验 CRUD + 状态机流转 |
| 分流引擎 | `services/bucketing_service.py` | MD5 哈希分桶 + 层管理 |
| 数据模拟器 | `services/data_simulator.py` | 用户画像 + 事件生成 + 效应注入 |
| 指标引擎 | `services/metrics_service.py` | 4 类指标定义 + 按用户/按天聚合 |
| 统计引擎 | `services/stats_service.py` + `utils/stats_utils.py` | t 检验 / z 检验 / 置信区间 / 样本量估算 |
| 可视化面板 | `ui/pages/*.py` + `ui/components/*.py` | 3 页面 Streamlit 交互式 UI |

### 技术栈

| 层 | 选型 |
|---|---|
| 前端框架 | Streamlit 1.23 |
| 数据库 | SQLite + SQLAlchemy ORM |
| 统计分析 | scipy + statsmodels |
| 数据处理 | pandas + numpy |
| 可视化 | plotly |
| 哈希分桶 | hashlib (MD5) |

---

## 2. 用户系统

### 2.1 首次使用——注册

启动平台后看到登录/注册页面：

1. 点击「注册」tab
2. 填写用户名（必填，全局唯一）
3. 填写密码（至少 6 位）
4. 填写邮箱（选填）
5. 点击「注册」

密码使用 **PBKDF2-SHA256** 哈希存储（10 万次迭代 + 32 字节盐值），不存储明文。

### 2.2 登录

在「登录」tab 输入用户名和密码，验证通过后进入主界面。

### 2.3 登录持久化

登录后 URL 会附带一个 session token（如 `?token=xxx`），24 小时内有效。关闭浏览器标签页后重新打开同一 URL，无需重新登录。

### 2.4 数据可见性

| 操作 | 权限 |
|------|------|
| 查看实验列表 | 所有人可见所有实验 |
| 查看实验详情 | 所有人可查看 |
| 分析实验结果 | 所有人可分析 |
| 创建实验 | 关联当前登录用户 |
| 操作实验（灰度/全量/暂停等） | 仅创建者可操作 |
| 删除实验 | 仅创建者可删除 |
| 模拟数据 | 仅创建者可为自己的实验生成数据 |

### 2.5 退出

进入「用户中心」→「账户安全」tab → 点击「退出登录」，会清除服务端 session 并跳转到登录页。

### 2.5 用户中心

导航栏「👤 用户中心」包含三个 tab：

| Tab | 功能 |
|-----|------|
| 📝 个人资料 | 查看基本信息 + **编辑资料**（部门、职位、邮箱、个人简介） |
| 🔒 修改密码 | 输入旧密码 + 新密码 + 确认 |
| ⚙️ 账户安全 | 退出登录 / 注销账户（二次确认） |

个人资料字段：

| 字段 | 说明 |
|------|------|
| 用户名 | 注册时设定，不可修改 |
| 部门 | 如"数据科学部" |
| 职位 | 如"高级数据分析师" |
| 邮箱 | 可编辑 |
| 个人简介 | 最多 300 字 |
| 注册时间 | 自动记录 |

### 2.6 退出与注销

进入「用户中心」→「⚙️ 账户安全」tab：

| 操作 | 说明 |
|------|------|
| 退出登录 | 清除 session，跳转登录页，**不删除数据** |
| 注销账户 | 二次确认后**永久删除**账户及所有实验数据，不可恢复 |

## 3. 环境准备与启动

### 2.1 依赖安装

```bash
pip install streamlit sqlalchemy scipy statsmodels pandas numpy plotly
```

### 2.2 启动平台

```bash
cd ab_platform
python -m streamlit run app.py --server.port 8501 --server.headless true
```

浏览器打开 `http://localhost:8501`，左侧边栏有三个导航页面：

```
🧪 AB实验平台
─────────────────
📋 实验管理      ← 创建、查看、操作实验
📊 实验详情      ← 分桶可视化、放量、数据模拟
📈 结果分析      ← 指标卡片、趋势图、置信区间
```

### 2.3 目录结构

```
ab_platform/
├── app.py                          # Streamlit 入口
├── config.py                       # 全局配置
├── database.py                     # 数据库连接
├── models.py                       # 5 张 ORM 表
├── services/
│   ├── experiment_service.py       # 实验管理
│   ├── bucketing_service.py        # 分流引擎
│   ├── data_simulator.py           # 数据模拟器
│   ├── metrics_service.py          # 指标引擎
│   └── stats_service.py            # 统计引擎
├── ui/
│   ├── pages/
│   │   ├── experiments.py          # 实验管理页
│   │   ├── experiment_detail.py    # 实验详情页
│   │   └── results.py              # 结果分析页
│   └── components/
│       ├── metric_card.py          # 指标卡片
│       └── charts.py               # 图表组件
└── utils/
    └── stats_utils.py              # 统计工具函数
```

---

## 4. 页面一：实验管理

### 3.1 实验列表（"实验列表" tab）

页面顶部有两个标签页，默认显示实验列表。

**筛选栏（列表顶部）：**

| 筛选项 | 选项 | 说明 |
|--------|------|------|
| 归属筛选 | 全部实验 / 我的实验 | 默认全部，切换后仅看自己创建的 |
| 状态筛选 | 全部状态 / 草稿 / 灰度 / 运行中 / 已暂停 / 已结束 | 按实验状态过滤 |
| 搜索框 | 文本搜索 | 按实验名称、编号、创建人搜索 |

**显示信息：**
- 实验名称 + 实验编号 + 创建人
- 当前状态（彩色标签：草稿/灰度放量/运行中/已暂停/已结束）
- 流量占比
- 起止时间
- 分组信息（如：对照组(50%) | 实验组(50%)）
- 操作按钮

**状态颜色含义：**

| 状态 | 颜色 | 含义 |
|------|------|------|
| 草稿 | 灰色 | 刚创建，未启动 |
| 灰度放量 | 橙色 | 小流量验证中 |
| 运行中 | 绿色 | 全量或指定流量运行 |
| 已暂停 | 蓝色 | 临时暂停 |
| 已结束 | 红色 | 实验终止，不可再操作 |

### 3.2 状态操作按钮

**所有操作均有二次确认弹窗**，点击按钮后会弹出确认/取消选项，防止误操作。

根据当前状态，系统自动显示可用操作：

| 当前状态 | 可用操作 |
|----------|----------|
| 草稿 | `▶ 灰度` — 启动小流量验证 / `🗑 删除` — 删除实验 |
| 灰度放量 | `▶ 全量` — 推全运行 / `⏸ 暂停` — 临时暂停 |
| 运行中 | `⏸ 暂停` — 临时暂停 / `⏹ 结束` — 终止实验 |
| 已暂停 | `▶ 恢复` — 恢复运行 / `⏹ 结束` — 终止实验 |

**确认流程：** 点击操作 → 列表底部出现警告提示 + 确认/取消按钮 → 点确认后执行
| 已结束 | 无（终态） |

**状态流转规则：**

```
draft ──→ ramp_up ──→ running ──→ ended
  │                    ↑    │
  └──→ ended          paused ─┘
                       ↑    │
                       └────┘
```

非法跳转（如 已结束→运行中）会弹出错误提示。

### 3.3 新建实验（"新建实验" tab）

点击「新建实验」标签页，填写以下信息：

| 字段 | 说明 | 示例 |
|------|------|------|
| 实验名称 | 必填 | `首页推荐算法优化` |
| 实验假设 | 描述预期效果 | `新推荐算法能提升用户点击率 5%` |
| 负责人 | 填写你的名字 | `张三` |
| 总流量占比 | 滑块 1%~100% | `10%`（灰度从 10% 开始） |
| 对照组名称 | 默认"对照组" | 可自定义 |
| 对照组流量 | 默认 50% | 与实验组之和会自动归一化 |
| 实验组名称 | 默认"实验组" | 可自定义 |
| 实验组流量 | 默认 50% | 与对照组之和会自动归一化 |

点击「创建实验」后，系统自动：
1. 创建并关联默认流量层
2. 在层内找到空闲桶区间
3. 按比例分配对照组和实验组的桶范围
4. 初始状态设为"草稿"

---

## 5. 页面二：实验详情

### 4.1 选择实验

顶部下拉框列出所有实验（含状态标签），选择后显示三个 tab：

### 4.2 基本信息 & 分流（tab 1）

**基本信息卡片：**
- 状态、总流量占比、实验组数
- 实验假设、负责人、起止时间

**分流可视化：**
- 堆叠柱状图展示各组在 0~9999 桶中占用的区间
- 每个组显示：名称、桶编号范围、桶数量、流量百分比

**分桶一致性测试：**
- 输入任意 User ID（如 `user_000001`）
- 显示该用户是否在实验内、分配到哪个组
- 自动验证：同一用户多次查询结果是否一致（哈希一致性）

**事件日志统计：**
- 按事件类型（exposure / page_view / click / purchase）显示计数
- 无数据时提示前往数据模拟 tab

### 4.3 渐进放量（tab 2）

仅当实验状态为"灰度放量"、"运行中"或"已暂停"时可操作。

- 拖动滑块调整流量百分比（1%~100%，步长 1%）
- 点击「确认调整」后重新分配桶区间，保留各组比例不变
- **放量建议：** 1% → 5% → 10% → 25% → 50% → 100%，每步至少观察 1 天

### 4.4 数据模拟（tab 3）

> 因为练习阶段没有真实数据，平台内置数据模拟器生成仿真的用户行为日志。

**可配置参数：**

| 参数 | 范围 | 默认值 | 说明 |
|------|------|--------|------|
| 模拟用户数 | 200~10000 | 2000 | 生成多少个虚拟用户 |
| 实验天数 | 1~60 | 14 | 实验运行天数 |
| 效应大小 (%) | 0~50 | 5 | 实验组相对对照组提升的百分比 |
| 预实验天数 | 0~30 | 7 | 实验开始前生成多少天历史数据 |

**模拟数据包含：**

| 特性 | 说明 |
|------|------|
| 用户画像 | 新/老用户(35%/65%)、活跃度(高/中/低)、8 个城市地域 |
| 活跃度加权 | 高活跃用户更高概率被选为日活，模拟真实 DAU 分布 |
| 周末效应 | 周末日活减少 ~27%，页面浏览减少 ~30% |
| 事件序列 | 每个用户访问触发：exposure → page_view(多次) → click(概率) → purchase(概率) |
| 效应注入 | 实验组的 click/purchase 概率按配置的 effect_size 倍率提升 |
| 预实验数据 | 实验开始前 N 天的历史数据，为后续 CUPED 做准备 |

点击「开始生成模拟数据」后，事件批量写入 `event_logs` 表（每批 500 条）。

**重新生成：** 如果该实验已有数据，会显示「清空旧数据并重新生成」按钮。

---

## 6. 页面三：结果分析

### 5.1 选择实验

顶部下拉框选择实验后，自动运行统计分析。

### 5.2 结论汇总

四个指标卡片展示整体情况：

| 卡片 | 说明 |
|------|------|
| 指标总数 | 当前定义的指标个数（默认 4 个） |
| 显著指标 | 有多少个指标 p < 0.05 |
| 显著北极星指标 | 显著指标中属于"北极星"类别的数量 |
| 实验结论 | 有显著提升 → "🔔 有显著提升" / 无 → "无显著差异" |

### 5.3 指标详情（可展开面板）

每个指标一个可展开面板，标题格式：`✅/⬜ 指标名 — 提升 +X.XX% (p=0.XXXX)`

展开后面板分为两栏：

**左栏 — 指标卡片（颜色编码）：**

| 颜色 | 含义 |
|------|------|
| 绿色背景 + 绿色边框 | 显著正向提升 |
| 红色背景 + 红色边框 | 显著负向劣化 |
| 灰色背景 + 灰色边框 | 不显著 |

卡片显示：
- 显著性图标 + 指标名称
- 指标类别（north_star / driver / guardrail）
- 检验方法（Welch's t-test / z-test）
- 对照组均值 / 实验组均值
- 相对提升百分比（正数绿色、负数红色）
- p 值 + 95% 置信区间

**右栏 — 时间序列趋势图：**
- 折线图按天展示各组指标走势
- 对照组蓝色、实验组红色

### 5.4 预设的 4 个指标

| 指标名称 | 类型 | 计算方法 | 分类 |
|----------|------|----------|------|
| 人均页面浏览数 | continuous | 每用户 page_view 事件数（COUNT）按组求均值 | driver |
| 点击率 | proportion | 有点击行为的用户数 / 有曝光的用户数 | north_star |
| 购买转化率 | proportion | 有购买行为的用户数 / 有曝光的用户数 | north_star |
| 人均购买金额 | continuous | 每用户 purchase 金额（SUM）按组求均值 | driver |

### 5.5 检验方法

| 指标类型 | 检验方法 | 说明 |
|----------|----------|------|
| continuous | Welch's t-test | 不假设两组方差相等 |
| proportion | z-test | 两样本比例检验 |

所有检验采用双侧检验，显著性水平 α = 0.05。

### 5.6 置信区间森林图

页面底部展示所有指标的 **相对提升百分比 + 95% 置信区间** 水平排列图：

- 每个指标一个点（点颜色：显著正向=绿 / 显著负向=红 / 不显著=灰）
- 水平误差线表示 95% CI 范围
- 虚线标注 lift = 0（零效应线）
- CI 不跨越零线 → 显著

### 5.7 样本量信息

页面底部显示每个指标的对照组和实验组实际样本量。

---

## 7. 完整操作流程示例

> 假设你要测试"新推荐算法是否提升用户点击率 5%"

### Step 1：创建实验

1. 进入「实验管理」→ 点击「新建实验」tab
2. 填写：名称 `推荐算法优化实验`，假设 `新算法提升点击率 5%`，负责人 `你的名字`
3. 流量设为 `10%`（灰度先跑 10%），组名保持默认
4. 点击「创建实验」

### Step 2：启动灰度

1. 切回「实验列表」tab
2. 找到刚创建的实验（状态：草稿）
3. 点击「▶ 灰度」→ 状态变为"灰度放量"

### Step 3：模拟数据

1. 进入「实验详情」→ 下拉选择该实验
2. 点击「数据模拟」tab
3. 设置：用户数 `2000`，实验天数 `14`，效应大小 `5`，预实验天数 `7`
4. 点击「开始生成模拟数据」
5. 提示生成成功（约 3~4 万条事件）

### Step 4：查看结果

1. 进入「结果分析」→ 下拉选择该实验
2. 查看结论汇总 → 展开指标详情
3. 观察点击率的 lift、p 值、置信区间
4. 查看时间序列趋势和森林图

### Step 5：放量决策

如果结果显著正向：
1. 进入「实验详情」→「渐进放量」tab
2. 将流量从 10% 调到 25% → 确认
3. 等待更多数据累积后继续观察

如果结果不显著或负向：
- 进入「实验管理」→ 点击「⏹ 结束」

---

## 8. AB 实验实践指南

> 本章汇总互联网大厂 AB 实验的实际运作经验，帮助你理解平台背后的方法论。

### 8.1 实验全生命周期

一个标准实验经历五个阶段，每个阶段的目标和决策标准完全不同：

```
草稿 → 灰度放量 → 正式运行 → 结束 → 归档
```

| 阶段 | 流量 | 时长 | 核心目标 | 决策标准 |
|------|------|------|----------|----------|
| 草稿 | 0% | — | 配置实验参数、评审 | — |
| 灰度放量 | 1%→5%→10% | 1~3 天 | **验证工程稳定性** | 无异常告警即可放量 |
| 正式运行 | 50%~100% | 7~14 天 | **验证业务效果** | p < 0.05 且效应方向正向 |
| 结束 | 0% | — | 总结、决策、上线或回滚 | — |

### 8.2 灰度 vs 正式——天壤之别

这是 AB 实验最容易被误解的地方。两者目标完全不同：

**灰度阶段（ramp_up）**

| 维度 | 说明 |
|------|------|
| 目的 | 确认新代码不会"炸"——工程上的安全验证 |
| 关注指标 | **护栏指标**：500 错误率、接口延迟、崩溃率、订单金额一致性 |
| 是否看 p 值 | **不看**。几百～几千用户的样本量不够做统计推断 |
| 决策逻辑 | "没有坏消息就是好消息" |
| 放量节奏 | 1%（1~2 小时）→ 5%（半天）→ 10%（1 天） |

真实案例：某推荐算法灰度 1% 时发现实验组 p99 延迟飙了 300ms——立刻回滚，根本没等到看点击率。这就是灰度的价值。

**正式运行（running）**

| 维度 | 说明 |
|------|------|
| 目的 | 验证业务假设——"新算法真的比旧算法好？" |
| 关注指标 | **北极星指标** + **驱动指标** |
| 是否看 p 值 | **必须看**。等样本量够、p < 0.05 才做结论 |
| 决策逻辑 | "有显著的好消息才算成功" |
| 典型时长 | 至少 7 天（覆盖完整周周期） |

**关键区别总结：**

```
灰度：别出事 → 推全量（质量把关）
正式：要有效 → 发结论（效果验证）
```

### 8.3 指标体系设计

互联网大厂通常将指标分为三层：

**第一层：北极星指标（North Star Metric）**

最能代表产品核心价值的指标，通常是**一个**。实验结论主要看它。

| 产品 | 北极星指标 |
|------|-----------|
| 电商 | 人均 GMV（成交总额） |
| 内容平台 | 人均使用时长 |
| 搜索 | 首点击率 / 搜索满意率 |
| 广告 | 千次展示收入（RPM） |

**第二层：驱动指标（Driver Metrics）**

拆解北极星指标的子指标，帮助理解"为什么"北极星变了。

- GMV = 转化率 × 客单价 × 流量 → 分析哪个因子在驱动变化
- 时长 = 启动次数 × 单次时长 → 新算法是让用户更频繁打开还是单次看更久？

**第三层：护栏指标（Guardrail Metrics）**

- 绝对不能显著劣化
- 即使北极星显著提升，护栏劣化也要回滚
- 典型：页面错误率、延迟、用户投诉率、退款率

**实验决策矩阵：**

| 北极星 | 护栏 | 决策 |
|--------|------|------|
| ✅ 显著正向 | ✅ 无劣化 | **上线** |
| ✅ 显著正向 | ❌ 显著劣化 | **回滚，查原因** |
| ⬜ 不显著 | ✅ 无劣化 | **延长实验或放弃** |
| ❌ 显著负向 | — | **立即回滚** |

### 8.4 流量分流原理

**层（Layer）与桶（Bucket）**

```
全部用户流量
    │
    ├── Layer 1（独立层，10000 桶）── 实验 A（10% = 1000 桶）
    │                                     ├── 对照组（500 桶，50%）
    │                                     └── 实验组（500 桶，50%）
    │
    ├── Layer 2（独立层，10000 桶）── 实验 B（20% = 2000 桶）
    │                                     ├── 对照组（1000 桶）
    │                                     └── 实验组（1000 桶）
    │
    └── Layer 3 ...
```

- **层间正交**：同一用户在不同层可能分到不同组，保证实验 A 和实验 B 互不干扰
- **层内互斥**：同一层内实验的桶区间不重叠，同一用户不会同时进两个实验
- **用户一致性**：同一用户 + 同一层 → 始终同一桶 → 始终同一组（哈希保证）

**流量百分比的实际含义**

| 设置 | 含义 |
|------|------|
| 10% | 全体 UV 中约 10% 参与实验（10000 桶中占 1000 桶） |
| 50% | 一半用户参与实验 |
| 100% | 全部用户参与实验（常用于已确认效果后的 AB 验证） |

### 8.5 实验需要多少用户？跑多久？

**样本量估算公式（简化版）**

每组的样本量取决于四个因素：

| 因素 | 符号 | 说明 |
|------|------|------|
| 基线转化率 | p₀ | 当前对照组的值 |
| 最小可检测效应 | δ | 你关心的最小提升幅度 |
| 显著性水平 | α | 通常 0.05 |
| 统计功效 | 1-β | 通常 0.8 |

粗略估算（α=0.05, β=0.2）：

| 基线 | MDE（最小可检测效应） | 每组需要样本量 |
|------|----------------------|---------------|
| 10%（转化率） | 1%（绝对提升） | ~14,000 |
| 10% | 2% | ~3,500 |
| 10% | 5% | ~600 |
| 50%（均匀概率） | 2% | ~9,600 |
| 50% | 5% | ~1,600 |

**经验法则：**
- 低转化率事件（如购买 2%）需要大量用户
- 高流量产品（日活百万级）：几天就够了
- 低流量产品（日活几千）：可能需要跑 2~4 周
- **至少覆盖一个完整周周期**（含周末和工作日）

### 8.6 实验决策框架

```
实验开始
    │
    ▼
[灰度 1%] ── 1 小时 ── 护栏正常？ ── 否 → 🔴 回滚
    │ 是
    ▼
[灰度 5%] ── 半天 ── 护栏正常？ ── 否 → 🔴 回滚
    │ 是
    ▼
[灰度 10%] ── 1 天 ── 护栏正常？ ── 否 → 🔴 回滚
    │ 是
    ▼
[正式 50%~100%] ── 7~14 天 ──
    │
    ├── 北极星显著正向 + 护栏正常 → 🟢 上线
    ├── 北极星显著正向 + 护栏劣化 → 🔴 回滚查原因
    ├── 所有指标不显著 → ⚠️ 延长/加大样本/放弃
    └── 北极星显著负向 → 🔴 立即回滚
```

### 8.7 常见误区

**误区 1：灰度阶段就看 p 值**
> 灰度样本量太小（可能只有几百人），p 值无意义。灰度只看工程指标：报错率、延迟、崩溃。

**误区 2：p > 0.05 就是"没有效果"**
> p > 0.05 只表示"没有足够证据说有效果"，不等于"证明没效果"。可能是样本量不够或效应太小。正确的做法是检查样本量是否够、考虑延长实验。

**误区 3：看到 p < 0.05 就立刻停止实验**
> "偷窥效应"（peeking）：每天看一次 p 值，等到 p < 0.05 就停止，会大幅增加假阳性率。正确做法：**事先定好实验时长**，到时间再下结论，中间不要频繁决策。

**误区 4：只看总体的 p 值，不看分群**
> 新算法可能对高活用户有正面效果，对新用户有负面效果。分群分析能发现这种"总体不显著但内部差异巨大"的情况。

**误区 5：不设护栏指标**
> 只看北极星不看护栏，可能导致"点击率 +5%，但页面崩溃率 +10%"的情况被忽略。

**误区 6：把相关性当因果**
> AA 实验（对照组 vs 对照组，效应=0）如果不通过——说明分流有偏差或指标计算有 bug。真正的 AB 实验前，应该先跑一次 AA 实验验证系统正确性。

### 8.8 大厂实验文化的核心原则

1. **假设驱动**：每个实验必须有明确的假设，不能"随便试试"
2. **一次一变**：每次只改变一个变量，否则不知道是哪个因素引起的效应
3. **预先注册**：实验目的、时长、成功标准在启动前定好，不能跑完再改
4. **结果可复现**：显著的实验结果应该能通过推全后再检验
5. **护栏优先**：任何护栏指标显著劣化，无论北极星多好，都回滚

---

## 9. 技术架构

### 分流算法

```
hash = MD5(user_id + salt) % 10000    # 映射到 0~9999 桶
bucket ∈ [group.start, group.end)     # 判断属于哪个实验组
```

- **层隔离：** 每层独立 10000 桶，不同层使用不同 salt
- **一致性：** 同一 user_id + 同一 salt → 始终同一桶 → 始终同一组
- **正交性：** 不同层的实验互不影响（用户在不同层可能分到不同组）

### 统计检验流程

```
event_logs
  → metrics_service (按 user_id 聚合 / 按天聚合)
    → stats_service (Welch's t-test / z-test)
      → 输出: {lift_pct, p_value, ci_lower, ci_upper, is_significant}
        → results.py 渲染卡片和图表
```

### 数据流

```
DataSimulator
  → 用户画像 + 效应注入 + 活跃度采样
    → event_logs (批量写入)
      → metrics_service (聚合)
        → stats_service (检验)
          → UI (渲染)
```

---

## 10. 数据唯一性控制

### 实验业务编号

每个实验创建时自动生成全局唯一的业务编号，格式：

```
EXP-{创建人缩写}-{日期}-{当日序号}
```

**示例：**
- `EXP-张三-20260604-001` — 张三在 2026-06-04 创建的第 1 个实验
- `EXP-张三-20260604-002` — 张三当天创建的第 2 个实验
- `EXP-李四-20260604-001` — 李四当天创建的第 1 个实验（不同创建人独立计数）

编号在实验列表中显示在名称下方，详情页顶部也可见。

### 各表唯一性约束汇总

| 表 | 唯一约束 | 方式 |
|---|---|---|
| experiments | `experiment_code` 全局唯一 | 列级 UNIQUE + INDEX |
| layers | `name` 全局唯一 | 列级 UNIQUE |
| experiment_groups | 同一实验内 `group_name` 不重复 | `UniqueConstraint(experiment_id, group_name)` |
| metric_definitions | `name` 全局唯一 | 列级 UNIQUE |
| event_logs | 无（日志表，允许重复） | — |

---

## 11. 数据库表结构

### users（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键（自增） |
| username | VARCHAR(50) | 用户名，UNIQUE |
| password_hash | VARCHAR(128) | PBKDF2-SHA256 哈希（格式：salt$hash） |
| email | VARCHAR(100) | 邮箱（选填） |
| created_at | DATETIME | 注册时间 |

### experiments（实验表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键（自增） |
| experiment_code | VARCHAR(50) | 业务编号，UNIQUE，如 EXP-zhan-20260604-001 |
| name | VARCHAR(200) | 实验名称 |
| creator_id | INTEGER | 外键 → users.id，创建者 |
| hypothesis | TEXT | 实验假设 |
| status | VARCHAR(20) | draft/ramp_up/running/paused/ended |

| total_traffic_pct | FLOAT | 总流量占比 (%) |
| start_date | DATE | 开始日期 |
| end_date | DATE | 结束日期 |
| layer_id | INTEGER | 外键 → layers |
| created_at | DATETIME | 创建时间 |

### experiment_groups（实验组表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| experiment_id | INTEGER | 外键 → experiments |
| group_name | VARCHAR(100) | 如"对照组""实验组" |
| bucket_range_start | INTEGER | 起始桶编号 |
| bucket_range_end | INTEGER | 结束桶编号 |
| traffic_pct | FLOAT | 该组流量占比 |

### layers（流量层表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| name | VARCHAR(100) | 层名称 |
| total_buckets | INTEGER | 总桶数，默认 10000 |
| description | VARCHAR(500) | 描述 |

### metric_definitions（指标定义表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| name | VARCHAR(200) | 指标名称 |
| metric_type | VARCHAR(20) | continuous/proportion/count |
| numerator_field | VARCHAR(100) | 分子事件类型 |
| denominator_field | VARCHAR(100) | 分母事件类型 |
| category | VARCHAR(50) | north_star/driver/guardrail |

### event_logs（事件日志表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| user_id | VARCHAR(100) | 用户 ID |
| experiment_id | INTEGER | 实验 ID |
| group_id | INTEGER | 实验组 ID |
| event_type | VARCHAR(50) | exposure/page_view/click/purchase |
| event_value | FLOAT | 事件数值（时长/金额等） |
| event_date | DATE | 事件日期 |
| timestamp | DATETIME | 时间戳 |

---

## 12. 常见问题

**Q: 如何注册新用户？**
A: 启动平台后自动显示登录/注册页面，点击「注册」tab，填写用户名和密码即可。

**Q: 其他人能看到我的实验吗？**
A: 不能。每个用户只能看到和操作自己创建的实验，数据完全隔离。

**Q: 实验编号的格式是什么？**
A: `EXP-{用户名缩写}-{YYYYMMDD}-{3位序号}`。编号自动生成，关联创建者用户名。

**Q: 可以创建两个同名实验吗？**
A: 可以，实验名称不强制唯一。但业务编号 `experiment_code` 是全局唯一的，用于精确标识每个实验。

**Q: 为什么人均页面浏览数总是显示为 ~1？**
A: 已修复。v1.0 中 page_view 由 AVG(event_value) 改为 COUNT 方式聚合，正确反映每用户浏览页面数。

**Q: 数据模拟中的效应大小是什么意思？**
A: 实验组相对对照组的提升幅度。如 effect_size=5 表示实验组的 click 概率为对照组的 1.05 倍。

**Q: 为什么效应设为 5% 了但结果不显著？**
A: 显著性受样本量、基线概率、方差共同影响。可尝试增大用户数、延长实验天数、或提高效应大小。

**Q: 可以创建多个实验吗？**
A: 可以。同一层的多个实验会自动找不重叠的桶区间，互不干扰。

**Q: 实验组流量之和超过 100% 会怎样？**
A: 系统自动按比例归一化，不会报错。

**Q: 如何清空数据重新开始？**
A: 删除 `ab_platform.db` 文件后重启 Streamlit，所有数据会重新初始化。

**Q: 可以导出分析结果为 Excel 吗？**
A: 当前版本暂不支持。后续迭代计划加入数据导出功能。

---

*本文档随版本迭代自动更新。最新版本：v3.0 (2026-06-05)*
